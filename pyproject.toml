[dependency-groups]
dev = [
  "ruff>=0.9.2"
]

[project]
dependencies = [
  "django>=5.1.5",
  "django-environ>=0.12.0"
]
description = "Transaction to Google Sheets"
name = "trantrac"
readme = "README.md"
requires-python = ">=3.13"
version = "0.1.0"
